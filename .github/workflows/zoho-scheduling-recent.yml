name: Zoho Scheduling Recent

on:
  schedule:
    - cron: '18 1,7,13,19 * * *'
  workflow_dispatch:

concurrency:
  group: zoho-scheduling-recent
  cancel-in-progress: false

jobs:
  run:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - name: Add random delay
        run: sleep $((RANDOM % 30 + 5))
      
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
        env:
          GIT_TRACE: 0
          GIT_CURL_VERBOSE: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm install
      - run: node src/zoho/zoho-scheduling-recent.js
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          ZOHO_CLIENT_ID: ${{ secrets.ZOHO_CLIENT_ID }}
          ZOHO_CLIENT_SECRET: ${{ secrets.ZOHO_CLIENT_SECRET }}
          ZOHO_REFRESH_TOKEN: ${{ secrets.ZOHO_REFRESH_TOKEN }}
          ZOHO_ACCOUNT_OWNER: ${{ secrets.ZOHO_ACCOUNT_OWNER }}
          ZOHO_SCHEDULING_APP_NAME: ${{ secrets.ZOHO_SCHEDULING_APP_NAME }}
          ZOHO_SCHEDULING_REPORT_NAME: ${{ secrets.ZOHO_SCHEDULING_REPORT_NAME }}
